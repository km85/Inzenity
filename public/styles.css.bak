:root {
  --bg: #0b0b0f;
  --surface: #15151a;
  --elevated: #1e1e24;
  --elevated-2: #26262e;
  --border: rgba(255, 255, 255, 0.08);
  --text: #f2f2f5;
  --text-secondary: #a9a9b6;
  --muted: #7b7b8c;
  --brand: #ff4d2e;
  --brand-2: #ff7a00;
  --brand-soft: rgba(255, 77, 46, 0.16);
  --brand-glow: rgba(255, 77, 46, 0.35);
  --success: #22c55e;
  --warning: #f59e0b;
  --danger: #ef4444;
  --shadow: 0 20px 50px rgba(0, 0, 0, 0.38);
  --shadow-soft: 0 10px 30px rgba(255, 77, 46, 0.15);
  --radius-sm: 14px;
  --radius-md: 20px;
  --radius-lg: 28px;
  --radius-xl: 36px;
  --radius-full: 999px;
  --header-gradient: linear-gradient(135deg, #1a1a20 0%, #0d0d11 100%);
  --hero-gradient: linear-gradient(135deg, rgba(255, 77, 46, 0.92), rgba(255, 122, 0, 0.78));
  --carbon: repeating-linear-gradient(45deg, rgba(255,255,255,0.02) 0px, rgba(255,255,255,0.02) 1px, transparent 1px, transparent 10px);
}

[data-theme="light"] {
  --bg: #f4f5f7;
  --surface: #ffffff;
  --elevated: #ffffff;
  --elevated-2: #f8f8fb;
  --border: rgba(0, 0, 0, 0.08);
  --text: #111216;
  --text-secondary: #4b4b57;
  --muted: #6b7280;
  --brand: #d72638;
  --brand-2: #ff5722;
  --brand-soft: rgba(215, 38, 56, 0.10);
  --brand-glow: rgba(215, 38, 56, 0.20);
  --shadow: 0 16px 40px rgba(20, 20, 31, 0.10);
  --shadow-soft: 0 10px 24px rgba(215, 38, 56, 0.12);
  --header-gradient: linear-gradient(180deg, #ffffff 0%, #fff7f8 100%);
  --hero-gradient: linear-gradient(135deg, rgba(215, 38, 56, 0.96), rgba(254, 106, 93, 0.82));
  --carbon: none;
}

* {
  box-sizing: border-box;
}

html, body {
  margin: 0;
  padding: 0;
  min-height: 100%;
}

body {
  font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
  background: var(--bg);
  color: var(--text);
  line-height: 1.45;
  -webkit-font-smoothing: antialiased;
}

a {
  color: inherit;
  text-decoration: none;
}

button, input, select, textarea {
  font: inherit;
  color: inherit;
}

.hidden {
  display: none !important;
}

.eyebrow, .section-kicker, .header-kicker {
  font-size: 11px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.14em;
  color: var(--brand-2);
  margin: 0 0 8px;
}

.muted {
  color: var(--muted);
}

.text-secondary {
  color: var(--text-secondary);
}

/* Login */
.login-wrap {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 24px;
  background:
    var(--carbon),
    radial-gradient(circle at top left, var(--brand-glow), transparent 34%),
    var(--bg);
}

.login-card {
  width: min(100%, 460px);
  padding: 28px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);
}

.login-card h1 {
  margin: 0 0 10px;
  font-size: 30px;
  letter-spacing: -0.02em;
}

.landing-panel {
  display: grid;
  gap: 10px;
  margin: 20px 0;
}

.landing-item {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: center;
  padding: 13px 14px;
  border-radius: var(--radius-md);
  background: var(--elevated);
  border: 1px solid var(--border);
}

.landing-item strong {
  font-size: 13px;
}

.landing-item span {
  color: var(--muted);
  text-align: right;
}

.demo-strip {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-bottom: 18px;
}

/* Buttons */
.pill, .ghost-button, .primary-button, .danger-button, .status-button,
.sidebar-link, .sidebar-button, .drawer-link, .member-nav-link, .theme-toggle {
  border: 1px solid var(--border);
  border-radius: var(--radius-full);
  padding: 11px 15px;
  background: var(--surface);
  cursor: pointer;
  transition: transform 0.12s ease, box-shadow 0.15s ease, background 0.15s ease;
}

.pill:hover, .ghost-button:hover, .status-button:hover, .drawer-link:hover, .member-nav-link:hover {
  transform: translateY(-1px);
}

.pill {
  background: var(--elevated);
  color: var(--text-secondary);
}

.primary-button {
  background: var(--brand);
  color: white;
  border-color: var(--brand);
  box-shadow: var(--shadow-soft);
  font-weight: 600;
}

.primary-button:hover {
  transform: translateY(-1px);
  box-shadow: 0 12px 28px var(--brand-glow);
}

.danger-button, .danger-link {
  color: var(--brand);
}

.ghost-button {
  background: transparent;
  color: var(--text-secondary);
}

.helper-text {
  margin: 14px 0 0;
  color: var(--muted);
  font-size: 14px;
}

.input-group {
  display: grid;
  gap: 6px;
  margin-bottom: 14px;
}

.input-group label {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
}

.input-group input, .input-group textarea, .input-group select {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  background: var(--elevated);
  color: var(--text);
  outline: none;
}

.input-group input:focus, .input-group textarea:focus, .input-group select:focus {
  border-color: var(--brand);
  box-shadow: 0 0 0 3px var(--brand-soft);
}

/* Member shell */
.member-shell {
  min-height: 100vh;
  padding: 18px 16px 116px;
  background:
    var(--carbon),
    radial-gradient(circle at top left, var(--brand-glow), transparent 30%),
    var(--bg);
}

.member-header {
  max-width: 520px;
  margin: 0 auto 18px;
  display: grid;
  grid-template-columns: 48px 1fr 48px;
  align-items: center;
  gap: 12px;
}

.icon-button, .brand-chip, .avatar-circle {
  width: 46px;
  height: 46px;
  border-radius: 50%;
  border: 1px solid var(--border);
  display: grid;
  place-items: center;
  background: var(--surface);
  box-shadow: var(--shadow);
  color: var(--text);
}

.icon-button {
  font-size: 22px;
}

.brand-chip, .brand-nav-button {
  background: var(--brand);
  color: white;
  font-weight: 800;
  border: none;
  letter-spacing: -0.04em;
}

.member-header-copy p {
  margin: 0;
}

.member-header-copy h1 {
  margin: 2px 0 0;
  font-size: 22px;
}

.member-main {
  max-width: 520px;
  margin: 0 auto;
}

.content-section + .content-section {
  margin-top: 20px;
}

/* Hero */
.home-hero-card {
  margin-bottom: 20px;
}

.hero-banner, .event-registration-cover, .news-cover, .merch-cover, .vendor-cover {
  border-radius: var(--radius-lg);
}

.hero-banner {
  min-height: 240px;
  padding: 22px;
  display: flex;
  align-items: end;
  color: white;
  box-shadow: var(--shadow-soft);
  position: relative;
  overflow: hidden;
}

.hero-banner::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, transparent 30%, rgba(0,0,0,0.55) 100%);
  pointer-events: none;
}

.hero-banner-copy {
  position: relative;
  z-index: 1;
}

.hero-banner-copy h2 {
  margin: 8px 0;
  font-size: 28px;
  line-height: 1.15;
}

.hero-banner-copy p, .hero-support {
  margin: 0;
  color: rgba(255,255,255,0.85);
  font-size: 14px;
}

.hero-badge, .soft-chip {
  display: inline-flex;
  align-items: center;
  padding: 7px 12px;
  border-radius: var(--radius-full);
  background: rgba(255,255,255,0.18);
  color: white;
  font-size: 12px;
  font-weight: 700;
}

.soft-chip {
  background: var(--brand-soft);
  color: var(--brand);
}

.hero-dots {
  display: flex;
  gap: 8px;
  justify-content: center;
  margin-top: 12px;
}

.hero-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  border: none;
  background: var(--border);
  cursor: pointer;
}

.hero-dot.active {
  background: var(--brand);
  box-shadow: 0 0 8px var(--brand-glow);
}

.section-title-row {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: end;
  margin-bottom: 12px;
}

.section-title-row h3 {
  margin: 0;
  font-size: 22px;
}

/* Grids */
.sponsor-grid, .news-grid, .merch-grid, .event-stack, .vendor-grid {
  display: grid;
  gap: 14px;
}

.sponsor-grid {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.sponsor-card, .news-card, .merch-card, .event-registration-card, .community-card,
.member-card, .vendor-card, .form-card, .dashboard-card, .metric-card, .detail-card,
.event-detail-shell, .login-card, .record-modal-card, .car-profile-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);
}

.sponsor-card {
  padding: 14px;
  display: grid;
  gap: 8px;
}

.sponsor-logo {
  width: 48px;
  height: 48px;
  border-radius: var(--radius-sm);
  display: grid;
  place-items: center;
  font-weight: 800;
  color: white;
  font-size: 14px;
}

.sponsor-card strong {
  font-size: 15px;
}

.sponsor-card span {
  color: var(--muted);
  font-size: 13px;
}

.news-card, .merch-card, .event-registration-card, .vendor-card, .sponsor-card {
  overflow: hidden;
  cursor: pointer;
  transition: transform 0.12s ease, box-shadow 0.15s ease;
}

.news-card:hover, .merch-card:hover, .event-registration-card:hover, .vendor-card:hover, .sponsor-card:hover {
  box-shadow: 0 0 0 1px var(--brand), var(--shadow);
}

.news-card:active, .merch-card:active, .event-registration-card:active, .vendor-card:active, .sponsor-card:active {
  transform: scale(0.985);
}

.hero-banner {
  cursor: grab;
  user-select: none;
  -webkit-user-select: none;
}

.hero-banner:active {
  cursor: grabbing;
}

.news-cover, .merch-cover, .event-registration-cover, .vendor-cover {
  min-height: 130px;
  background: var(--hero-gradient);
  position: relative;
}

.news-body, .merch-body, .event-registration-body, .vendor-body, .community-card,
.member-card, .detail-card, .car-profile-card {
  padding: 18px;
}

.news-meta, .vendor-meta {
  display: flex;
  justify-content: space-between;
  font-size: 12px;
  color: var(--muted);
  margin-bottom: 8px;
}

.news-card h4, .merch-card h4, .event-registration-card h4, .vendor-card h4,
.community-card h3, .member-card strong, .detail-card h3, .car-profile-card h3 {
  margin: 0 0 8px;
  font-size: 17px;
}

.community-card {
  display: grid;
  gap: 12px;
}

.member-card-top {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.large-avatar {
  width: 64px;
  height: 64px;
  font-size: 24px;
  background: var(--brand);
  color: white;
  border: none;
}

.member-detail-grid, .car-detail-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
  margin-top: 18px;
}

.member-detail-item, .car-detail-item {
  padding: 14px;
  border-radius: var(--radius-md);
  background: var(--elevated);
  border: 1px solid var(--border);
}

.member-detail-item span, .car-detail-item span {
  display: block;
  color: var(--muted);
  font-size: 12px;
  margin-bottom: 4px;
}

.member-detail-item strong, .car-detail-item strong {
  font-size: 14px;
  color: var(--text);
}

.compact-grid {
  margin-bottom: 18px;
}

.event-meta, .stat-row, .vendor-actions {
  color: var(--muted);
  font-size: 14px;
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.tag-row {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin: 12px 0;
}

.info-tag {
  display: inline-flex;
  align-items: center;
  padding: 7px 10px;
  border-radius: var(--radius-full);
  background: var(--brand-soft);
  color: var(--brand);
  font-size: 12px;
  font-weight: 600;
}

.pill-group {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.status-button {
  background: var(--elevated);
  color: var(--text-secondary);
}

.status-button.active {
  background: var(--brand);
  color: white;
  border-color: var(--brand);
  box-shadow: var(--shadow-soft);
}

.rsvp-summary {
  margin-top: 14px;
  padding: 14px;
  border-radius: var(--radius-md);
  background: var(--elevated);
  border: 1px solid var(--border);
}

.toolbar {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-bottom: 12px;
}

/* Drawer */
.drawer-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.55);
  z-index: 20;
  backdrop-filter: blur(2px);
}

.side-drawer {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  width: min(84vw, 320px);
  padding: 24px 18px;
  background: var(--surface);
  box-shadow: 0 20px 40px rgba(0,0,0,0.45);
  z-index: 21;
  display: flex;
  flex-direction: column;
}

.drawer-top {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 24px;
}

.drawer-top p {
  margin: 4px 0 0;
}

.drawer-menu {
  display: grid;
  gap: 10px;
}

.drawer-link, .theme-toggle {
  border-radius: var(--radius-md);
  text-align: left;
  padding: 14px 16px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.drawer-link.danger-link {
  color: var(--brand);
}

.drawer-link .icon, .theme-toggle .icon {
  font-size: 18px;
  margin-right: 10px;
}

/* Bottom nav */
.member-bottom-nav {
  position: fixed;
  left: 50%;
  bottom: 18px;
  transform: translateX(-50%);
  width: min(calc(100vw - 24px), 520px);
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  align-items: center;
  gap: 6px;
  padding: 10px;
  border-radius: var(--radius-xl);
  background: rgba(21, 21, 26, 0.92);
  border: 1px solid var(--border);
  box-shadow: 0 18px 40px rgba(0, 0, 0, 0.35);
  z-index: 15;
  backdrop-filter: blur(12px);
}

[data-theme="light"] .member-bottom-nav {
  background: rgba(255, 255, 255, 0.96);
}

.member-nav-link {
  background: transparent;
  border: none;
  text-align: center;
  color: var(--muted);
  padding: 10px 4px;
  border-radius: var(--radius-md);
  font-size: 11px;
  font-weight: 600;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
}

.member-nav-link .nav-icon {
  font-size: 18px;
  line-height: 1;
}

.member-nav-link.active {
  background: var(--brand-soft);
  color: var(--brand);
}

/* Vendor screen */
.vendor-card {
  overflow: hidden;
}

.vendor-cover {
  min-height: 110px;
}

.vendor-body p {
  color: var(--text-secondary);
  font-size: 14px;
  margin: 0 0 12px;
}

.vendor-actions {
  align-items: center;
}

.vendor-whatsapp {
  margin-left: auto;
  background: var(--success);
  color: white;
  border-color: var(--success);
  font-weight: 600;
}

.vendor-search {
  width: 100%;
  padding: 12px 14px;
  border-radius: var(--radius-full);
  border: 1px solid var(--border);
  background: var(--surface);
  color: var(--text);
  margin-bottom: 14px;
  outline: none;
}

.vendor-search:focus {
  border-color: var(--brand);
  box-shadow: 0 0 0 3px var(--brand-soft);
}

/* Car profile / My Zenix */
.car-profile-card {
  margin-bottom: 16px;
}

.car-hero {
  height: 110px;
  background: var(--hero-gradient);
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  display: grid;
  place-items: center;
  font-size: 48px;
}

.car-form-actions {
  display: flex;
  gap: 10px;
  margin-top: 14px;
}

/* Admin */
.admin-body {
  background: var(--bg);
}

.dashboard-shell {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 280px 1fr;
}

.sidebar {
  background: var(--surface);
  color: var(--text);
  padding: 24px 18px;
  display: flex;
  flex-direction: column;
  gap: 22px;
  border-right: 1px solid var(--border);
}

.sidebar .eyebrow {
  color: var(--brand-2);
}

.sidebar-brand h1, .sidebar-brand p, .sidebar-user strong, .sidebar-user span {
  margin: 0;
}

.sidebar-user, .sidebar-brand {
  padding: 4px 8px;
}

.sidebar-nav {
  display: grid;
  gap: 8px;
}

.sidebar-link, .sidebar-button {
  width: 100%;
  text-align: left;
  border-color: var(--border);
  background: var(--elevated);
  color: var(--text);
}

.sidebar-link.active {
  background: var(--brand);
  color: white;
  border-color: var(--brand);
  box-shadow: var(--shadow-soft);
}

.sidebar-actions {
  display: grid;
  gap: 10px;
  margin-top: auto;
}

.dashboard-main {
  padding: 28px;
}

.dashboard-header {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: start;
  margin-bottom: 20px;
}

.overview-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 16px;
  margin-bottom: 20px;
}

.metric-card {
  padding: 18px;
  background: var(--surface);
  border: 1px solid var(--border);
}

.metric-card h3 {
  font-size: 28px;
  margin: 0;
  color: var(--brand);
}

.metric-card .eyebrow {
  margin-bottom: 8px;
}

.dashboard-content {
  display: grid;
  grid-template-columns: minmax(0, 1.55fr) minmax(320px, 420px);
  gap: 20px;
}

.dashboard-card {
  padding: 20px;
  background: var(--surface);
  border: 1px solid var(--border);
}

.editor-card {
  align-self: start;
  background: var(--surface);
  border: 1px solid var(--border);
}

.modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.55);
  z-index: 40;
  backdrop-filter: blur(2px);
}

.record-modal {
  position: fixed;
  inset: 0;
  display: grid;
  place-items: center;
  padding: 20px;
  z-index: 41;
}

.record-modal-card {
  width: min(100%, 560px);
  max-height: min(90vh, 760px);
  overflow-y: auto;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);
  padding: 22px;
}

.table-heading {
  margin-bottom: 12px;
  display: flex;
  justify-content: space-between;
  align-items: start;
  gap: 12px;
}

.table-wrap {
  overflow-x: auto;
}

table {
  width: 100%;
  border-collapse: collapse;
}

th, td {
  text-align: left;
  padding: 12px 10px;
  border-bottom: 1px solid var(--border);
  vertical-align: top;
  color: var(--text);
}

th {
  color: var(--muted);
  font-size: 13px;
  font-weight: 600;
}

.split {
  display: grid;
  gap: 14px;
}

.empty-state {
  padding: 32px 16px;
  text-align: center;
  color: var(--muted);
}

/* Gradient utility classes */
.hero-cover-1 { background: linear-gradient(135deg, #ff4d2e, #ff7a00); }
.hero-cover-2 { background: linear-gradient(135deg, #d72638, #5c1623); }
.hero-cover-3 { background: linear-gradient(135deg, #ff7a00, #ff4d2e); }
.news-cover-1 { background: linear-gradient(135deg, #2a1b1b, #d72638); }
.news-cover-2 { background: linear-gradient(135deg, #1f1b16, #ff7a00); }
.news-cover-3 { background: linear-gradient(135deg, #1b1b1b, #d72638); }
.merch-cover-red { background: linear-gradient(135deg, #d72638, #ff4d2e); }
.merch-cover-dark { background: linear-gradient(135deg, #1a1a1a, #3d0f0f); }
.merch-cover-sand { background: linear-gradient(135deg, #2a221b, #d98d66); }
.vendor-cover-1 { background: linear-gradient(135deg, #1a1a20, #3b0f0f); }
.vendor-cover-2 { background: linear-gradient(135deg, #1a1a20, #2b1d0f); }
.vendor-cover-3 { background: linear-gradient(135deg, #1a1a20, #0f1f1b); }
.vendor-cover-4 { background: linear-gradient(135deg, #1a1a20, #0f1b2f); }
.sponsor-tone-1 { background: linear-gradient(135deg, #ff4d2e, #ff7a00); }
.sponsor-tone-2 { background: linear-gradient(135deg, #ff7a00, #ffb700); }
.sponsor-tone-3 { background: linear-gradient(135deg, #991b1b, #ff4d2e); }
.sponsor-tone-4 { background: linear-gradient(135deg, #d72638, #ff7a72); }

[data-theme="light"] .hero-cover-1 { background: linear-gradient(135deg, #d72638, #ff7a72); }
[data-theme="light"] .hero-cover-2 { background: linear-gradient(135deg, #b81f2f, #5c1623); }
[data-theme="light"] .hero-cover-3 { background: linear-gradient(135deg, #ff7a00, #ff4d2e); }
[data-theme="light"] .news-cover-1 { background: linear-gradient(135deg, #ffd4db, #f15a6d); }
[data-theme="light"] .news-cover-2 { background: linear-gradient(135deg, #fce9d4, #ff8b5e); }
[data-theme="light"] .news-cover-3 { background: linear-gradient(135deg, #fdecee, #d72638); }
[data-theme="light"] .merch-cover-red { background: linear-gradient(135deg, #d72638, #ff7b72); }
[data-theme="light"] .merch-cover-dark { background: linear-gradient(135deg, #2c1219, #6e1f2c); }
[data-theme="light"] .merch-cover-sand { background: linear-gradient(135deg, #f4d9c6, #d98d66); }
[data-theme="light"] .vendor-cover-1 { background: linear-gradient(135deg, #e5e7eb, #fee2e2); }
[data-theme="light"] .vendor-cover-2 { background: linear-gradient(135deg, #e5e7eb, #ffedd5); }
[data-theme="light"] .vendor-cover-3 { background: linear-gradient(135deg, #e5e7eb, #d1fae5); }
[data-theme="light"] .vendor-cover-4 { background: linear-gradient(135deg, #e5e7eb, #dbeafe); }
[data-theme="light"] .sponsor-tone-1 { background: linear-gradient(135deg, #d72638, #ff7b72); }
[data-theme="light"] .sponsor-tone-2 { background: linear-gradient(135deg, #ff7f50, #ffb26f); }
[data-theme="light"] .sponsor-tone-3 { background: linear-gradient(135deg, #991b1b, #d72638); }
[data-theme="light"] .sponsor-tone-4 { background: linear-gradient(135deg, #b81f2f, #f87171); }

@media (max-width: 980px) {
  .dashboard-shell { grid-template-columns: 1fr; }
  .overview-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .dashboard-content { grid-template-columns: 1fr; }
  .sidebar { border-right: none; border-bottom: 1px solid var(--border); }
}

@media (max-width: 640px) {
  .overview-grid, .member-detail-grid, .car-detail-grid, .sponsor-grid { grid-template-columns: 1fr; }
  .dashboard-main { padding: 18px; }
  .member-shell { padding: 16px 14px 110px; }
  .member-bottom-nav { gap: 4px; }
  .member-nav-link { font-size: 10px; padding: 8px 2px; }
  .member-nav-link .nav-icon { font-size: 16px; }
}

@media (prefers-color-scheme: light) {
  body:not([data-theme]) { --bg: #f4f5f7; --surface: #ffffff; --text: #111216; }
}
.car-photo, .avatar-photo, .avatar-photo.small {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: inherit;
}

.car-photo-hero {
  padding: 0;
  overflow: hidden;
}

.upload-row {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 14px 0;
  margin: 0 18px;
  border-top: 1px solid var(--border);
}

.upload-row.compact {
  padding: 10px 0;
  margin: 0;
  border-top: none;
  border-bottom: 1px solid var(--border);
  margin-bottom: 14px;
}

.upload-label {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  border-radius: var(--radius-full);
  background: var(--elevated);
  border: 1px solid var(--border);
  color: var(--text-secondary);
  font-size: 13px;
  cursor: pointer;
}

.upload-icon {
  font-size: 16px;
}

.hidden-file-input {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  border: 0;
}

.upload-remove {
  margin-left: auto;
  font-size: 12px;
  padding: 8px 12px;
}

.member-card-top .avatar-circle,
.drawer-top .avatar-circle,
.brand-chip {
  overflow: hidden;
}

.large-logo {
  width: 96px;
  height: 96px;
  font-size: 28px;
  border-radius: var(--radius-md);
  margin: 0 auto 18px;
  display: grid;
  place-items: center;
  font-weight: 800;
  color: white;
}

.detail-cover {
  min-height: 220px;
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
}

.detail-placeholder {
  margin-top: 18px;
  padding: 18px;
  border-radius: var(--radius-md);
  background: var(--elevated);
  border: 1px dashed var(--border);
  text-align: center;
}

.detail-placeholder p {
  margin: 0 0 6px;
  color: var(--text-secondary);
}
